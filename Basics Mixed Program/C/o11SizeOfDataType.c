// Size of Data Type
#include<stdio.h>
void main(){
    char a;
    int b; 
    float c; 
    double d;

    printf("\nSize of character is %d byte ", sizeof(a) );
    printf("\nSize of integer is %ld byte ", sizeof(b) );
    printf("\nSize of float is %d byte ", sizeof(c) );
    printf("\nSize of double is %d byte ", sizeof(d) );
    
    
}