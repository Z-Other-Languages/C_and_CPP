#include<iostream>
using namespace std;
int main(){

    int a=45;
    int b=30;
    // Ternary Operator means if condition is true then
    // first value before colon will print else last value
    cout<< ( (a<b) ? a:b );
}